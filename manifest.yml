# Auto-generated by Spring Boot Universal Migrator
# Binds the converted project to existing CF services for manual verification
# Usage: cf push -f manifest.yml
---
applications:
  - name: session-1-cfef4bd9
    memory: 1G
    disk_quota: 2G
    instances: 1
    timeout: 180
    path: target/*.jar
    buildpacks:
      - java_buildpack_offline
    services:
      - doc-ai-rabbitmq
      - doc-ai-postgres
    env:
      JAVA_HOME: /opt/java
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 21.+ } }'
    health-check-type: http
    health-check-http-endpoint: /actuator/health
